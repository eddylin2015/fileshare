
extends ./base.pug

block content
  div#divid
    table
      each row in books 
        tr 
          each elm,eidx in row.slice(0,-1) 
            if eidx==1 
              td  
                a(href=`/GCFin/${elm.replace(":","_")}`)= elm
            else 
              td= elm
  script.
    console.log("ok!")